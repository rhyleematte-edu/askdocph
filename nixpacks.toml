[phases.setup]
nixPkgs = ["php", "nodejs", "composer"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader", "npm install"]

[phases.build]
cmds = ["npm run production"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"

